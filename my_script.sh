#! /usr/bin/env sh

set -e                          # <-- abort if any command fails

echo "hello"                    # <-- print "hello" on stdout
exit 0                          # <-- exit with success
# exit 1                          # <-- exit with failure (anything != 0)
